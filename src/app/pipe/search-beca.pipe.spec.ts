import { SearchBecaPipe } from './search-beca.pipe';

describe('SearchBecaPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBecaPipe();
    expect(pipe).toBeTruthy();
  });
});